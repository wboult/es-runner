rootProject.name = 'docker-opensearch-shared-cluster-fixture'
include('app', 'search')

dependencyResolutionManagement {
    repositories {
        mavenCentral()
    }
}

includeBuild('@REPO_ROOT@')
